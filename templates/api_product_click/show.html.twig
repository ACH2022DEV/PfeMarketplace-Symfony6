{% extends 'base.html.twig' %}

{% block title %}ApiProductClick{% endblock %}

{% block body %}
    <h1>ApiProductClick</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ api_product_click.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ api_product_click.date ? api_product_click.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IpTraveler</th>
                <td>{{ api_product_click.ipTraveler }}</td>
            </tr>
            <tr>
                <th>IpLocation</th>
                <td>{{ api_product_click.ipLocation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_api_product_click_index') }}">back to list</a>

    <a href="{{ path('app_api_product_click_edit', {'id': api_product_click.id}) }}">edit</a>

    {{ include('api_product_click/_delete_form.html.twig') }}
{% endblock %}
