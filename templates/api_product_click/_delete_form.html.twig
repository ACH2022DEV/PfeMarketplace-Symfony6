<form method="post" action="{{ path('app_api_product_click_delete', {'id': api_product_click.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ api_product_click.id) }}">
    <button class="btn">Delete</button>
</form>
